<h1><%= @task.title%></h1>
<p><%= @task.details%></p>
<%= link_to "Back to the main page", tasks_path %>
<div mt 2>
  <%= link_to "Edit a task", "/tasks/#{@task.id}/edit" %>
</div>
<p><%= link_to 'Delete',
       "/tasks",
       data: { turbo_method: :delete, turbo_confirm: 'You sure?' } %></p>
